<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loanscrefia.member.edu.repository.EduRepository">
   
   	<!-- 회원사 시스템 > 모집인 등록 : 엑셀 업로드 시 교육이수번호 체크 -->
   	<select id="plEduNoCheck" parameterType="edu" resultType="int">
   		SELECT 
   			COUNT(1)
   		FROM TB_LC_EDU01
   		<where>
   			AND PL_M_NAME 	= #{plMName}
   			AND PL_M_Z_ID 	= SUBSTRING(REPLACE(#{plMZId}, '-', ''),1,7)
   			AND PL_PRODUCT 	= #{plProduct}
   			<if test="careerTyp.equals('1')">
   			AND PL_EDU_NO 	= #{plEduNo}
   			</if>
   			<if test="careerTyp.equals('2')">
   			AND PL_CRF_NO 	= #{plEduNo}
   			</if>
   		</where>
   	</select>
   
   
   
   
   
</mapper> 